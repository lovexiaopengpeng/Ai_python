# 宝塔面板详细部署指南

## 📋 服务器信息
- 公网IP: 111.230.110.33
- 宝塔面板地址: http://111.230.110.33:8888/tencentcloud
- 用户名: 90a387e8
- 密码: 988269904ff0

---

## 🚀 部署步骤（推荐方法：Python项目管理器）

### 第一步：登录宝塔面板
1. 在浏览器中打开: http://111.230.110.33:8888/tencentcloud
2. 输入用户名和密码登录
3. 绑定宝塔账号（首次登录需要）

### 第二步：安装Python项目管理器
1. 点击左侧菜单的 **"软件商店"**
2. 在搜索框输入 **"Python项目管理器"**
3. 点击 **"安装"** 按钮
4. 等待安装完成

### 第三步：上传项目文件
1. 点击左侧菜单的 **"文件"**
2. 进入目录 **/www/wwwroot/**
3. 创建一个新文件夹，命名为 **fastapi-project**
4. 进入该文件夹，上传所有项目文件：
   - main.py
   - requirements.txt
   - wechat_robot.py
   - news_hot.py
   - cn_stock.py
   - crypto.py
   - 其他所有项目文件

### 第四步：配置Python项目管理器
1. 点击左侧菜单的 **"软件商店"**
2. 找到刚安装的 **"Python项目管理器"**，点击 **"设置"**
3. 在Python项目管理器界面：
   - 点击 **"添加项目"**
   - 项目名称: fastapi-project
   - 路径: /www/wwwroot/fastapi-project
   - Python版本: 选择 Python 3.x (3.8 或更高版本)
   - 框架: 选择 fastapi
   - 启动文件: main.py
   - 启动命令: uvicorn main:app --host 0.0.0.0 --port 8000
4. 点击 **"确定"** 或 **"提交"**

### 第五步：安装项目依赖
1. 在Python项目管理器中，找到刚添加的项目
2. 点击项目后的 **"模块"** 或 **"依赖"** 按钮
3. 点击 **"添加模块"**，输入以下命令：
   - pip install -r requirements.txt
4. 或在项目详情中找到"安装依赖"功能
5. 等待依赖安装完成

### 第六步：启动项目
1. 在Python项目管理器中，点击项目后的 **"启动"** 按钮
2. 等待服务启动
3. 查看日志，确认启动成功

### 第七步：配置网站和反向代理
1. 点击左侧菜单的 **"网站"**
2. 点击 **"添加站点"**
3. 域名填写: 111.230.110.33
4. 其他选项保持默认
5. 点击 **"提交"**
6. 在站点列表中，找到刚添加的站点，点击 **"设置"**
7. 点击左侧 **"配置文件"** 选项卡
8. 在配置文件中添加反向代理配置：

```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

9. 点击 **"保存"**

### 第八步：开放防火墙端口
1. 点击左侧菜单的 **"安全"**
2. 添加端口规则:
   - 端口: 80, 443, 8888, 8000
   - 策略: 放行
3. 点击 **"添加"**

---

## 🌐 访问您的项目

部署完成后，访问以下地址：

- 网站首页: http://111.230.110.33
- API文档: http://111.230.110.33/docs

---

## 🛠️ 备选方案：使用终端部署

如果您更喜欢用终端，可以使用以下方法：

### 1. 打开终端
在宝塔面板中点击左侧菜单的 **"终端"**

### 2. 进入项目目录
```bash
cd /www/wwwroot/fastapi-project
```

### 3. 创建虚拟环境
```bash
python3 -m venv venv
source venv/bin/activate
```

### 4. 安装依赖
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 5. 测试运行
```bash
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 6. 使用screen后台运行
```bash
# 安装screen（如果未安装）
yum install screen -y

# 创建新screen会话
screen -S fastapi

# 运行项目
cd /www/wwwroot/fastapi-project
source venv/bin/activate
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4

# 按 Ctrl+A+D 退出screen会话
```

---

## 📞 常见问题解决

### 问题1：无法访问网站
- 检查端口是否已开放（安全组和防火墙）
- 检查宝塔面板的安全设置

### 问题2：Python项目管理器无法启动
- 检查Python版本
- 查看项目日志中的错误信息
- 确认依赖都已安装

### 问题3：依赖安装失败
- 尝试更新pip: pip install --upgrade pip
- 使用国内镜像源: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

---

## 📝 管理命令

```bash
# 查看服务状态
systemctl status fastapi-project

# 启动服务
systemctl start fastapi-project

# 停止服务
systemctl stop fastapi-project

# 重启服务
systemctl restart fastapi-project

# 查看日志
journalctl -u fastapi-project -f
```

---

## 🎉 完成！

您的FastAPI项目已经成功部署！
- 🌐 访问地址: http://111.230.110.33
- 📚 API文档: http://111.230.110.33/docs
